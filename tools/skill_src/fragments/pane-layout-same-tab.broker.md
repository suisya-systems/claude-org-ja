- **全ペインを同一タブ scope に配置する**: broker surface には `new_tab` / `focus_pane` が無く（意図的除外）、pane-addressed 操作（`mcp__org-broker__list_panes` / `send_message` / `inspect_pane` 等）は契約 Surface 4.2 の **SINGLE-TAB MUST**（cross-tab addressing は `pane_not_found` を返す論理 addressing scope）に従う。したがってディスパッチャー・キュレーター・全ワーカーは同一タブ scope に配置する（broker では各ペインが detached 独立セッション / 別 GUI ウィンドウで起動し視覚的に積み重なる訳ではないが、addressing が同一タブ scope に解決される点は契約上同一）。（`ORG_TRANSPORT=renga` では `list_panes` / `focus_pane` / `send_message` / `inspect`（CLI）が現在フォーカス中のタブのペインしか扱えず、`new_tab` でワーカーを別タブに置くとディスパッチャー側から addressable でなくなる制約 — 2026-04-20 判明、renga 本体での解決は suisya-systems/renga#71 — が同じ同一タブ要件を導く）
